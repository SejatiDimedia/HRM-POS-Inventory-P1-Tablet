import 'package:flutter/material.dart';

class EditPayroll extends StatelessWidget {
  const EditPayroll({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      child: Scaffold(
        body: Center(
          child: Text('Edit Payroll'),
        ),
      ),
    );
  }
}
